# Blackbeam OS — environment for the n8n auto-quote workflow
# Copy real secret values into n8n (Settings → Variables). DO NOT commit secrets.

# --- already provisioned (safe, non-secret) ---
SUPABASE_URL=https://ivxbznqouzlpskgfyxiy.supabase.co
BLACKBEAM_ORG_ID=05695bd2-4999-4544-a18b-eaf0afa9c651
# Supabase publishable key (safe for the OS frontend; RLS protects data):
# sb_publishable_v8Oyy2PUHdf8bpu6a2uvwg_-UsCibYO

# --- you must fill these (SECRET — get from each dashboard) ---
SUPABASE_SERVICE_KEY=      # Supabase → Project Settings → API → service_role  (server-only!)
ANTHROPIC_API_KEY=         # console.anthropic.com → API Keys
RESEND_API_KEY=            # resend.com → API Keys (verify your sending domain)
